# Copier en .env (gitignoré) et compléter.

# Mot de passe du service postgres local (docker-compose.yml racine, image pgvector/pgvector) -
# base de connaissances RAG en développement local. Cf. rag-llm/.env.template pour DATABASE_URL.
POSTGRES_PASSWORD=

# EN SOMMEIL depuis la migration pgvector — uniquement nécessaire si le service ngrok du
# docker-compose.yml racine est réactivé (`docker-compose --profile ngrok up -d ngrok`), cf.
# docs/simulation-prod-ngrok.md.
# NGROK_AUTHTOKEN=
